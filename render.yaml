services:
  - type: web
    name: forze-stats-backend
    env: node
    buildCommand: cd server && npm install && npx puppeteer browsers install chrome
    startCommand: cd server && node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3001
